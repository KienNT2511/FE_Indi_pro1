# URL gốc của API backend (kèm /api/v1).
# - Nếu host frontend có rewrite /api → backend: BỎ TRỐNG (dùng tương đối /api/v1).
# - Nếu gọi thẳng backend khác origin: đặt URL đầy đủ như dưới.
VITE_API_URL=https://your-backend.onrender.com/api/v1
